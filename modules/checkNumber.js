/**
 * 
 * @param {*} TryNumber 시도할 숫자입니다.
 * @param {*} targetNumber 타겟이 될 숫자입니다.
 */
export const checkNumber = (TryNumber,targetNumber) => {
  if(TryNumber== targetNumber){
    return {state}
  }
}